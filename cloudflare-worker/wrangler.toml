name = "safestreet-edge-alerts"
main = "src/index.js"
compatibility_date = "2024-09-01"

# Free tier settings (100,000 req/day, bypasses Firebase Spark plan external API restrictions)
[vars]
ENVIRONMENT = "production"
ENABLE_EDGE_CACHE = "true"
